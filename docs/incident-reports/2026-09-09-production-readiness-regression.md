# 2026-09-09 生产就绪优化回归事故报告

## 事故结论

本次问题属于严重的前端完整性回归。问题不是业务功能自然失效，而是在生产化和组件库接入调整中，没有先建立现有行为与呈现基线，错误地把“未理解的现有能力”当成可裁剪项，同时混用了自动解析、手动导入和依赖预构建策略。结果覆盖登录、导航头部、图标、认证持久化、动态路由和多个深层演示页面。

本次修复坚持恢复既有能力，不重做视觉、不更换交互、不扩展业务范围。对照 `origin/main`、问题提交和当前产物后，确认共有 18 类退化或高风险点；运行时代码没有被整页删除，但存在功能开关、样式入口、图标生成和加载链路被切断，实际影响等同于功能丢失。

需要特别更正：前一次 `729c03e` 修复提交仍把“路由最终能打开”和“遮罩覆盖视口”当成通过，没有核验首次呈现、实际透明度、验证码图标语义与可感知延迟，因此错误地宣布事故已闭环。用户复验发现的问题成立，该次验收本身也是本事故的一部分。

## 影响范围与量化

| 类别           | 直接影响                                                                                        | 根因                                                                                               | 修复结果                                                                 |
| -------------- | ----------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------ |
| 登录功能面     | 7 个辅助能力被关闭：记住我、忘记密码、人机验证、验证码登录、扫码登录、第三方登录、注册          | 在未获业务授权时修改 `LOGIN_FEATURES` 默认值                                                       | 8 个登录能力开关全部恢复，密码登录保持不变                               |
| 登录演示数据   | 默认账号和密码 2 项被移除                                                                       | 把生产数据治理错误应用到开发演示入口                                                               | 恢复 `CHENY / 123456`，生产远端模式仍由环境契约约束                      |
| 登录背景       | 深色背景依赖原子 CSS 生成，加载异常时退化为透明                                                 | 缺少组件自身的视觉兜底                                                                             | 保留原子类并增加 `#181818` 样式兜底，未改变原视觉                        |
| 头部组件样式   | 全局搜索、消息中心、语言、主题、功能引导共 5 条样式链丢失                                       | 删除全量 CSS 后，又用手动运行时导入绕过 resolver 的 `sideEffects`                                  | 恢复 resolver 按需解析；手动导入的组件显式加载对应样式                   |
| 组件包图标     | 发布产物运行时使用的 17 个 `i-mdi:*` 类无法被应用侧扫描                                         | 删除 UnoCSS safelist，且发布产物不在默认源码扫描范围                                               | 恢复精确 17 项白名单，不扩大扫描和首屏负担                               |
| 历史图标组件   | 5 个源码文件中的 `<Icon>` 使用失去解析器                                                        | 移除旧插件时同时删除了仍在使用的解析能力                                                           | 直接解析到现有 `@iconify/vue`，不恢复废弃插件依赖                        |
| 代码高亮       | 高亮初始化被移除                                                                                | 插件清理时误判为无用能力                                                                           | 恢复按需语言注册和应用初始化                                             |
| 认证持久化     | 跨标签页、刷新和浏览器重启的既有登录语义被改成单标签会话                                        | 未经产品决策将 `localStorage` 改成 `sessionStorage`                                                | 恢复持久化语义，并迁移事故版本留下的会话数据；密码字段继续清洗           |
| 登录后导航     | 顶部进度条长时间运行，菜单页迟迟无法进入                                                        | 菜单主契约同步等待辅助按钮/数据权限；首次加载时依赖重新发现又会强制刷新                            | 只等待菜单主契约，辅助权限默认拒绝并后台补齐；登录跳转等待完成           |
| 开发启动       | 从原有约 3–4 秒退化到约 9 秒                                                                    | 同步 Git 横幅、预加载插件、warmup 和过宽依赖预构建叠加                                             | 默认关闭同步横幅，删除重复预热；本机多次冷/热启动实测约 2.4–3.9 秒       |
| 深层按需页面   | 上传、截图、条码、Office、视频、VTable/Gantt 等页面可能在首次访问时报默认导出错误或触发整页刷新 | 组件包保持 ESM 按需加载，但若干传递依赖仍为 CommonJS                                               | 仅预构建已确认的 CJS 桥接依赖，VTable 等重包继续按路由懒加载             |
| 验证码标识     | 人机验证入口由拼图图标退化为箭头文字                                                            | 组件包改变默认图标，并用逻辑或运算错误覆盖宿主有意传入的空字符串                                   | `0.11.7` 恢复 🧩，改用空值合并保留图标模式，独立样式入口包含内部依赖     |
| 首次布局帧     | 首次登录后布局样式错乱，刷新后才恢复                                                            | 原子 CSS 只等待懒模块进入 Vite 管道，布局先于关键样式生成                                          | 构建期只扫描布局与首页关键路径；第一帧和稳定帧几何一致                   |
| 引导遮罩       | 引导页全黑，只有刷新或退出后恢复                                                                | 向 Driver.js 传入 `overlayOpacity: undefined` 覆盖其 `0.7` 默认值                                  | 未配置时不传该字段；实测 SVG 和计算透明度均为 `0.7`                      |
| 菜单与登录延迟 | 登录和首次菜单点击长时间占用进度条                                                              | 登录成功后才加载布局；分组菜单双重跳转；普通页面无点击前预取                                       | 验证期间空闲预热、提交时暂停 3D、叶子菜单意图预取并移除重复跳转          |
| 按需入口泄漏   | 首次进入级联选择器意外请求整套组件库、编辑器、图表及图标依赖                                    | `import { type X }` 被 Vite 保留为根入口副作用导入；少量运行时助手仍从根入口读取                   | 纯类型统一为 `import type`，运行时助手改用 `/C_Form`，并增加全源码门禁   |
| 地图运行时     | 地图页连续弹出 `map is not a function`，4 个地图实例均无法显示                                  | Leaflet 的 CommonJS/UMD 导出在 Vite 按需链路下可能包裹于 `default`，组件直接把模块命名空间当作 API | `0.11.8` 统一解析两种模块形态并校验核心 API；宿主精确预构建 Leaflet 桥接 |
| 甘特图卸载竞态 | 快速离开首次加载中的甘特图页面后，异步模块仍向已卸载容器创建实例                                | 异步导入完成后没有再次核对组件存活状态、初始化版本和容器身份                                       | `0.11.8` 增加卸载、版本与容器三重守卫，失效初始化不再产生实例            |

`origin/main` 到当前分支仍有 5 份旧设计草案和 2 个废弃 Markdown 实现文件被删除。5 份设计草案已由现行主题架构文档收口，不参与运行；旧 Markdown 插件及主题样式已由 `md-editor-v3` 路径替代。恢复后的 `src/utils/unocss/icon-safelist.ts` 会随本次修复提交，因此不再是运行时删除项。

## 根因分析

1. 没有在优化前冻结登录页、头部弹层、认证持久化和动态路由的行为基线。
2. 把组件“可被导入”误当成组件“样式和运行时依赖也已完整接入”，忽略 resolver `sideEffects` 只对自动解析生效。
3. 把减少依赖预构建理解为完全放弃深层依赖互操作，没有逐路由验证组件包内传递依赖。
4. 启动优化同时引入多套预热手段，且没有用冷启动、热启动和首次登录导航三个独立指标验收。
5. 类型通配声明掩盖了真实接口问题，导致静态检查未能提前暴露部分风险。
6. 第一次事故修复仍以“最终可达”为验收条件，127 条路由遍历没有设置交互时延和首帧稳定阈值，因而把数秒等待也计为通过。
7. 组件包的单组件样式入口没有把内部组合组件视为发布契约，应用删除全量样式兜底后才暴露遗漏。
8. 可选配置值未经清洗直接透传给第三方库，`undefined` 与“未提供”的语义差异没有被测试覆盖。
9. 将组件库从整包预构建切换为深层按需加载后，没有扫描纯类型导入的实际编译产物，导致一处旧写法把根入口重新带回页面依赖图。
10. 浏览器巡检只统计未捕获异常，没有采集组件通过 `message.error()` 上报的错误，也没有断言地图、二维码、甘特图等组件已经真正就绪，因此地图故障被错误标记为通过。

## 修复与防复发措施

- 新增 `tests/ui-regression-contract.test.ts`，固定登录能力、演示凭据、背景兜底、5 个头部组件样式、手动组件样式、17 个运行时图标、代码高亮、导航非阻塞和依赖互操作契约。
- 删除遮蔽 Naive UI 的通配声明，应用与 Vite 配置均进入真实类型门禁。
- 自动解析组件统一交给官方 resolver；确需运行时手动导入的组件必须同位置显式导入样式，避免多个隐式事实源。
- 依赖预构建采用“标准 ESM 重包保持懒加载、已确认 CJS 桥接依赖精确包含”的边界，禁止再次添加全量页面预热。
- 普通 `dev` 不运行同步 Git 横幅；需要分支信息时显式使用 `dev:banner`。
- 后续涉及视觉或交互删改，必须先获得业务确认，并至少覆盖登录、首页、全部头部弹层和全路由浏览器巡检。
- 浏览器验收同时记录首帧与稳定帧几何、点击到路由完成耗时、进度条清理时间和全局 DOM 清理，禁止再把“超时前最终出现”计为性能通过。
- 组合组件发布时必须验证独立 JS、CSS、DTS 和 SSR 入口；宿主不得依赖另一个全量样式入口偶然兜底。
- 应用运行时代码禁止从组件库根入口取值；类型只能使用显式 `import type`。回归测试会扫描全部 `src/**/*.{ts,vue}`，避免按需边界再次被一行导入击穿。
- 浏览器巡检同时采集 `.n-message--error` 的整个生命周期，并对地图容器、二维码画布和甘特图实例设置组件级就绪断言；只出现页面外壳不再视为通过。

## 验证证据

- 组件包 `0.11.8` 发布门禁：71 项测试（216 个断言）、TypeScript、ESLint/Oxlint、JS/CJS/DTS/CSS 入口、SSR、消费端类型与包体预算通过；npm 正式产物与 Git 标签均已复核。
- 登录页：8 项能力、默认凭据、`rgb(24, 24, 24)` 背景、🧩 验证图标和原有交互入口保留；验证码按钮计算样式为 `flex`，图标尺寸 `20×20`。
- 全新存储的开发态登录实测：Mock 接口为 180ms，点击提交到正确布局第一帧约 479ms；首帧与稳定帧布局区域均为 `1920×1080`，主内容区域均为 `1700×930`。
- 普通菜单抽样点击的路由完成耗时约 30–62ms；未发现重复跳转、卡死进度条或运行时异常。
- 修复前级联选择器首次进入可触发约 2932 个请求（其中约 1802 个 Lucide、282 个 AntV 模块）；修复后该页面只加载自身、`C_Cascade`、标题组件和地区数据链，不再请求组件库根入口。
- 功能引导首步遮罩属性值与计算值均为 `0.7`，高亮区域与品牌 Logo 匹配，不再出现全黑遮罩。
- 生产预览登录页导航约 340ms，持久会话冷刷新到首页约 876ms；搜索面板、消息中心和五步引导均完成打开、逐步前进与全局 DOM 清理验证。
- 全新开发态浏览器中，二维码首次路由解析为 186ms，5 个二维码全部绘制为 640ms；地图首次路由解析为 111ms，4 个 Leaflet 实例全部就绪为 648ms。二者均无错误消息和运行时异常。
- 甘特图在组件外壳挂载后立即返回首页，等待异步依赖完成仍保持首页且无 `container is undefined`、错误消息或未捕获异常。
- 修正后的开发态巡检覆盖 127 条路由：全部页面均可达、无组件错误消息、无运行时异常；其中 125 条满足 2 秒冷路由预算，AntV X6（4.27 秒）和 Markdown 编辑器（10.03 秒）因开发态首次转换 931/1937 个模块超过预算，列为性能观察项而非功能通过项。正式构建结果另行验证。
- 正式构建的 127 条路由全部通过，平均路由解析 79ms、P95 176ms、最大 322ms，0 个运行时异常、0 个组件错误消息；AntV X6 和 Markdown 正式路由解析分别为 175ms、154ms，证实开发态超时来自首次模块转换而非生产产物退化。
- 最终门禁通过：Oxlint、ESLint、应用/Vite TypeScript、50 项测试、8,608 modules 生产构建、包体预算、冻结锁文件解析和 `git diff --check`。

## 责任与后续边界

本次事故由优化过程缺少完整基线和错误裁剪造成。后续不再以“代码更少”作为单独成功标准；只有现有功能、交互、样式、启动、导航、类型、构建和全路由运行均通过，才视为优化完成。Element Plus、移动端或新能力扩展不属于本次修复范围。
